.govuk-inset-text
  markdown:
    The form builder does not keep track of its contents so to ensure
    the error summary correctly links to the first #{input_type} `link_errors`
    must be set to `true`.
